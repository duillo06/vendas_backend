# TLS automático via Let's Encrypt (HTTP-01) — sem plugin DNS.
# Wildcard (*.dominio) exige DNS-01; no MVP liste os tenants abaixo.
# foodservice.app é trocado pelo remote-deploy.sh

{
	email admin@foodservice.app
}

api.foodservice.app {
	encode gzip
	reverse_proxy nginx:80
}

admin.foodservice.app {
	encode gzip
	reverse_proxy nginx:80
}

# Storefront demo — adicione um bloco por tenant novo
demo.foodservice.app {
	encode gzip
	reverse_proxy nginx:80
}

# Exemplo próximo cliente:
# pizzaria-joao.foodservice.app {
# 	encode gzip
# 	reverse_proxy nginx:80
# }
